<!-- views/administracion/confirmarEliminacion.ejs -->

<%- include('../partials/header', { title: 'Confirmar Eliminación' }) %>
<%- include('../partials/navbar') %>
<%- include('../partials/sidebar') %>
<%- include('../partials/content-wrapper-open') %>

<div class="container-xxl flex-grow-1 container-p-y">
  <div class="row justify-content-center">
    <div class="col-md-8">
      <div class="card">
        <div class="card-header">
          <h5 class="mb-0">Confirmar Eliminación</h5>
        </div>
        <div class="card-body">
          <p>La tabla <strong><%= tabla %></strong> no contiene una columna de <strong>ELIMINADO</strong> ni una columna que contenga <strong>ESTATUS</strong>. ¿Estás seguro de que deseas eliminar este registro de manera permanente?</p>
          <form action="/administracion/<%= tabla %>/eliminar/<%= id %>/confirmar" method="POST">
            <button type="submit" class="btn btn-danger me-2">Sí, eliminar definitivamente</button>
            <a href="/administracion/<%= tabla %>" class="btn btn-secondary">Cancelar</a>
          </form>
        </div>
      </div>
    </div>
  </div>
</div>

<%- include('../partials/footer-text') %>
<%- include('../partials/footer') %>
<%- include('../partials/content-wrapper-close') %>